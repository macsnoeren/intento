# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example
# De compose-sjabloon hoort juist wél in git (het bevat geen geheimen, alleen uitleg); het echte
# `.env.docker` blijft door de regel hierboven buiten.
!.env.docker.example

# Database (dev/test) — nooit committen; opnieuw op te bouwen via migraties/seed
*.db
*.db-journal
*.sqlite

# Gegenereerde Prisma-client (opnieuw op te bouwen via `prisma generate`)
server/src/generated/

# Test / coverage
coverage/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/

# Compose-omgeving met geheimen (fase 19); het sjabloon `.env.docker.example` staat hierboven expliciet
# uitgezonderd en hoort wél in git.
.env.docker
